export interface Post {
  imgSrc: string;
  date: string;
  header: string;
  body: string;
}

export interface Glance {
  campuses: number,
  foundingYear: number,
  classrooms: number,
  parents: number,
  passRate: number,
}