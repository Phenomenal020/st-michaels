/** @type {import('next').NextConfig} */
const nextConfig = {
  // images: {
  //   domains: ["www.shutterstock.com", "images.pexels.com"],
  // },
  // output: "export",
};

module.exports = nextConfig;